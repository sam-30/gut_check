[build]
  command   = "npm run build"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — serve index.html for any path (no 404 on refresh)
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
